/** Rough model prices (USD per 1M tokens) for the evaluation cost estimate only; adjust to the current price list. */
export const EVAL_PRICE_INPUT_PER_MILLION = 0.1;
export const EVAL_PRICE_OUTPUT_PER_MILLION = 0.4;
