import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync}from'node:fs';import{PGlite}from'@electric-sql/pglite';import{inferredLanguage,requestsNoAutomaticReplies}from'./client-cards.service.js';import{summaryDue}from'./owner-summary.service.js';
test('client preference detection is limited to language and explicit automation opt-out',()=>{assert.equal(inferredLanguage('שלום'),'he');assert.equal(inferredLanguage('Привет'),'ru');assert.equal(inferredLanguage('Hello'),'en');assert.equal(requestsNoAutomaticReplies('Не отвечай мне автоматически, пожалуйста'),true);assert.equal(requestsNoAutomaticReplies('Когда вы отвечаете?'),false);});
test('summary frequency and owner local delivery time are respected',()=>{const base:any={time_zone:'Asia/Jerusalem',behavior:{summary_frequency:'weekly',summary_time:'09:00',summary_weekday:1}};assert.equal(summaryDue(base,new Date('2026-09-14T05:59:00Z')),null);assert.deepEqual(summaryDue(base,new Date('2026-09-14T06:00:00Z'))?.days,7);assert.equal(summaryDue(base,new Date('2026-09-15T06:00:00Z'))?.periodKey,'weekly:2026-09-14');assert.equal(summaryDue({...base,behavior:{...base.behavior,summary_frequency:'off'}},new Date('2026-09-14T06:00:00Z')),null);});
test('040 deletes a client card while retaining its conversation messages',async()=>{const db=new PGlite();try{await db.exec(readFileSync('supabase/migrations/001_phase1_schema.sql','utf8').replace('create extension if not exists pgcrypto;',''));await db.exec(readFileSync('supabase/migrations/20260910120000_040_client_cards_and_owner_summaries.sql','utf8'));const t='10000000-0000-4000-8000-000000000001',c='20000000-0000-4000-8000-000000000001';await db.query("insert into tenants(id,name,phone)values($1,'T','1')",[t]);await db.query("insert into clients(id,tenant_id,phone,whatsapp_jid)values($1,$2,'123@lid','123@lid')",[c,t]);const conv=await db.query<{id:string}>('insert into conversations(tenant_id,client_id)values($1,$2)returning id',[t,c]);await db.query("insert into messages(tenant_id,conversation_id,from_me,body)values($1,$2,false,'private')",[t,conv.rows[0]!.id]);await db.query('delete from clients where id=$1',[c]);assert.equal((await db.query('select id from messages')).rows.length,1);assert.equal((await db.query<{client_id:string|null}>('select client_id from conversations')).rows[0]!.client_id,null);}finally{await db.close();}});
