import assert from'node:assert/strict';import test from'node:test';import{GeminiEmbeddingProvider}from'../providers/embedding/gemini-embedding.provider.js';
test('Gemini embedding uses versioned batched retrieval tasks and configured dimensions',async()=>{const original=globalThis.fetch;let body:any,url='';globalThis.fetch=async(input,init)=>{url=String(input);body=JSON.parse(String(init?.body));return Response.json({embeddings:[{values:[3,4]}],usageMetadata:{promptTokenCount:2}})};try{const provider=new GeminiEmbeddingProvider('secret','gemini-embedding-001',2),result=await provider.embed(['price list'],'RETRIEVAL_DOCUMENT');assert.match(url,/gemini-embedding-001:batchEmbedContents/);assert.equal(body.requests[0].taskType,'RETRIEVAL_DOCUMENT');assert.equal(body.requests[0].outputDimensionality,2);assert.deepEqual(result.vectors[0],[.6,.8]);assert.equal(result.inputTokens,2)}finally{globalThis.fetch=original}});
